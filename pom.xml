<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.appjars</groupId>
	<artifactId>appjars-configuration-manager-demo</artifactId>
	<name>Configuration Manager - Demo</name>
	<version>2.0.0</version>
	<packaging>jar</packaging>

	<organization>
		<name>AppJars</name>
		<url>https://www.appjars.com</url>
	</organization>
	<inceptionYear>2023</inceptionYear>

	<licenses>
		<license>
			<name>Apache 2</name>
			<url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
			<distribution>repo</distribution>
		</license>
	</licenses>

	<properties>
		<appjars.configuration-manager.version>2.0.0</appjars.configuration-manager.version>
		<error.window.vaadin.version>4.2.0</error.window.vaadin.version>
		<antler.tour.version>1.1.0</antler.tour.version>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<java.version>21</java.version>
		<maven.compiler.source>21</maven.compiler.source>
		<maven.compiler.target>21</maven.compiler.target>
		<vaadin.version>25.2.0</vaadin.version>
		<spring.boot.version>4.0.4</spring.boot.version>
	</properties>

	<repositories>
		<!-- The order of definitions matters. Explicitly defining central here
			to make sure it has the highest priority. -->

		<!-- Main Maven repository -->
		<repository>
			<id>central</id>
			<url>https://repo.maven.apache.org/maven2</url>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
		</repository>
		<!-- Repository used by many Vaadin add-ons -->
		<repository>
			<id>Vaadin Directory</id>
			<url>https://maven.vaadin.com/vaadin-addons</url>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
		</repository>
		<!-- Public repository hosting the AppJars artifacts -->
		<repository>
			<id>appjars</id>
			<url>https://maven.appjars.com</url>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
		</repository>
	</repositories>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>com.vaadin</groupId>
				<artifactId>vaadin-bom</artifactId>
				<version>${vaadin.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-dependencies</artifactId>
				<version>${spring.boot.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>

		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>com.vaadin</groupId>
			<artifactId>vaadin-spring-boot-starter</artifactId>
		</dependency>
		<dependency>
			<groupId>com.vaadin</groupId>
			<artifactId>vaadin-dev</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<optional>true</optional>
		</dependency>

		<!-- Configuration Manager: public API (model, business, flow) plus the
			implementations Spring wires at runtime. -->
		<dependency>
			<groupId>com.appjars</groupId>
			<artifactId>appjars-configuration-manager-model</artifactId>
			<version>${appjars.configuration-manager.version}</version>
		</dependency>
		<dependency>
			<groupId>com.appjars</groupId>
			<artifactId>appjars-configuration-manager-business</artifactId>
			<version>${appjars.configuration-manager.version}</version>
		</dependency>
		<dependency>
			<groupId>com.appjars</groupId>
			<artifactId>appjars-configuration-manager-flow</artifactId>
			<version>${appjars.configuration-manager.version}</version>
		</dependency>
		<dependency>
			<groupId>com.appjars</groupId>
			<artifactId>appjars-configuration-manager-business-impl</artifactId>
			<version>${appjars.configuration-manager.version}</version>
		</dependency>
		<dependency>
			<groupId>com.appjars</groupId>
			<artifactId>appjars-configuration-manager-data-impl</artifactId>
			<version>${appjars.configuration-manager.version}</version>
		</dependency>

		<dependency>
			<groupId>com.flowingcode.vaadin.addons</groupId>
			<artifactId>error-window-vaadin</artifactId>
			<version>${error.window.vaadin.version}</version>
		</dependency>

		<!-- Guided tours -->
		<dependency>
			<groupId>org.vaadin.addons.antlerflow</groupId>
			<artifactId>antler-tour</artifactId>
			<version>${antler.tour.version}</version>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<scope>provided</scope>
		</dependency>

	</dependencies>

	<build>
		<defaultGoal>spring-boot:run</defaultGoal>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<version>${spring.boot.version}</version>
				<!-- A clean Vaadin build can exceed Spring Boot's default 30s start timeout. -->
				<configuration>
					<wait>500</wait>
					<maxAttempts>240</maxAttempts>
				</configuration>
			</plugin>

			<plugin>
				<groupId>com.vaadin</groupId>
				<artifactId>vaadin-maven-plugin</artifactId>
				<version>${vaadin.version}</version>
				<executions>
					<execution>
						<goals>
							<goal>prepare-frontend</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>license-maven-plugin</artifactId>
				<version>1.14</version>
				<configuration>
					<licenseName>apache_v2</licenseName>
					<addJavaLicenseAfterPackage>false</addJavaLicenseAfterPackage>
					<excludes>
						<exclude>main/bundles/**</exclude>
						<exclude>main/frontend/**</exclude>
					</excludes>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<!-- Production mode is activated using -Pproduction -->
			<id>production</id>
			<build>
				<plugins>
					<plugin>
						<groupId>com.vaadin</groupId>
						<artifactId>vaadin-maven-plugin</artifactId>
						<version>${vaadin.version}</version>
						<executions>
							<execution>
								<goals>
									<goal>build-frontend</goal>
								</goals>
								<phase>compile</phase>
							</execution>
						</executions>
						<configuration>
							<productionMode>true</productionMode>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>
